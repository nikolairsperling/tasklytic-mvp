import Link from "next/link";
import { AdminCard, PageHeader, StatusBadge } from "@/components/admin/ui";
import { prisma } from "@/lib/prisma";
import { getUserOffer } from "@/lib/user-offer";
export const dynamic="force-dynamic";
export default async function Page(){const [offer,smtp, appUrlSet, templates, videos, booking, prospects, campaigns]=await Promise.all([getUserOffer(),prisma.smtpSettings.count(),Promise.resolve(Boolean(process.env.NEXT_PUBLIC_APP_URL||process.env.PUBLIC_APP_URL)),prisma.landingpageTemplate.count(),prisma.videoAsset.count(),prisma.prospect.count({where:{OR:[{bookingUrl:{not:null}},{calendarUrl:{not:null}}]}}),prisma.prospect.count(),prisma.campaign.count()]);const steps=[["Angebote definieren",Boolean(offer),"/admin/setup/offers"],["SMTP verbinden",smtp>0,"/admin/settings/email"],["App URL setzen",appUrlSet,"/admin/settings/app"],["Landingpage Template erstellen",templates>0,"/admin/assets/landingpages"],["Erklärvideo hinterlegen",videos>0,"/admin/assets/videos"],["Buchungskalender verbinden",booking>0,"/admin/settings/booking"],["Ersten Prospect anlegen",prospects>0,"/admin/prospects"],["Erste Kampagne erstellen",campaigns>0,"/admin/campaigns"],["Testmail senden",smtp>0,"/admin/settings/email"]] as const;return <div><PageHeader title="Setup Center" description="Onboarding-Checkliste für ein funktionsfähiges Outreach-System."/><AdminCard><div className="space-y-3">{steps.map(([label,done,href])=><div key={label} className="flex items-center justify-between gap-4 rounded-2xl border border-white/10 p-4"><div><p className="font-medium text-ink">{label}</p><StatusBadge tone={done?"green":"amber"}>{done?"fertig":"offen"}</StatusBadge></div><Link href={href} className="rounded-xl border border-white/10 px-3 py-2 text-sm text-slate-200">Öffnen</Link></div>)}</div></AdminCard></div>}
